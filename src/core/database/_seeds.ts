import {Pipe} from "../../pipes/Pipe";

export const DATABASE = {

    [Pipe.name]: [
        {
            id: 1, // providing an id will prevent duplicates
            event: 'user-groups.29y8.user-joined',
            digest: 'data-extraction.fvh97.add-user'
        }
    ]

}
